package accesscontrol;

/**
 * Enum representing the different user roles in the system.
 * Using enum ensures type safety and avoids errors from string comparisons.
 */
public enum Role {
    ADMIN,
    STAFF,
    STUDENT
}